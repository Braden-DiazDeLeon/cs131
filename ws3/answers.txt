A1: ls -l > listing.txt && date >> listing.txt
A2: ls /nonexistent_dir 2> errors.txt
A3: ls /nonexistent_dir /etc > output.txt 2> errors.txt
A4: ls /nonexistent_dir /etc &> combined.txt
A5: ls /nonexistent_dir /etc 2>/dev/null; terminal shows only the /etc listing because the error is redirected to /dev/null. /dev/null deletes anything written to it. Its like a trash
A6: sort fruits.txt | uniq
A7: sort fruits.txt | uniq -c | sort -nr
A8: cat server.log | grep -c ERROR
A9: cat server.log | grep ERROR > errors_only.txt
A10: grep -oE '^[A-Z]+' server.log | sort | uniq -c | sort -nr
A11: grep -oE '"state":"[^"]+"' /data/yelp/yelp_academic_dataset_business.json | sort | uniq -c | sort -nr | head -5
B1: id output: uid=12345(username) gid=12345(groupname) groups=12345(groupname),100(users). uid is my user ID. Gid is my primary group ID.
B2: fruits.txt permissions are -rw-r--r--. Owner rw- is read and write. group r-- is read only. other r-- is read only.
B3: umask was 0022. A new file starts at 666, so 666 - 022 is 644 meaning  -rw-r--r--. We check with: echo "top secret" > secret.txt and ls -l secret.txt
B4: chmod 000 secret.txt; cat secret.txt gave "Permission denied"; chmod u+rw secret.txt
B5: chmod 640 fruits.txt. This is 640
B6: find . -type f -user "$(whoami)" -perm -u=w
